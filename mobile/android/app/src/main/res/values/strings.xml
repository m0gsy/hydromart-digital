<?xml version='1.0' encoding='utf-8'?>
<resources>
    <!--
      app_name, title_activity_main, package_name and custom_url_scheme are NOT here:
      they differ per binary and are generated by app/build.gradle from the
      `hydromartAppId` / `hydromartAppName` Gradle properties. Declaring them here as
      well is a duplicate-resource build failure, which is the loud outcome — the quiet
      one would be shipping the Ops build labelled "Hydromart".
    -->
</resources>
