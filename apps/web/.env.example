# Base URL of the API gateway. All requests go through it (single ingress).
NEXT_PUBLIC_API_URL=http://localhost:8080
# Google OAuth web client id. Set to show the "Continue with Google" button on
# login/register (backend /auth/google verifies the returned id token). Blank = hidden.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
