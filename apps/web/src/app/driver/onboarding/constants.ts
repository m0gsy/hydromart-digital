// Shared here (not in page.tsx): Next's production page-type check rejects named
// exports from a page module that aren't route segment config.
export const ONBOARDED_KEY = 'hydromart_driver_onboarded';
