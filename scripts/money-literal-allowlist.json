{
  "apps/web/src/app/dashboard/suppliers/page.tsx:134 onTimeRate >= 0.9": "Badge colour band, green above 90% on-time and amber below. No money moves on it and no server disagrees with it — the rate itself comes from the API and is printed beside the badge.",
  "apps/web/src/app/hq/webhooks/page.tsx:86 deliveryRatePct >= 99": "Badge colour band for webhook delivery health. Presentation of a number the API sends; nothing is priced, paid or charged from it.",
  "apps/web/src/lib/dictionaries/en/courierFix.ts:54 Rp 3,000": "LIVE BUSINESS NUMBER, and possibly a promise nothing keeps: a peak-hour bonus of Rp 3.000 per delivery stated to couriers. Needs confirming against payroll before it is either interpolated or removed.",
  "apps/web/src/lib/dictionaries/en/dashboard.ts:130 Rp2,000": "Presentation. An input-format example (\"mis. -2000 untuk potongan Rp2.000\") teaching the field how to be typed, not quoting a rule.",
  "apps/web/src/lib/dictionaries/en/help.ts:31 Rp 1,000": "LIVE BUSINESS NUMBER, not presentation. The points earn rate is the per-depot `earnRateRupiah` tunable — /dashboard/loyalist already reads it and interpolates it. This customer FAQ hardcodes Rp 1.000, so a depot that changes the rate leaves this sentence lying. Allowlisted to record it, not to excuse it: fixing it needs the setting fetched on the help screen.",
  "apps/web/src/lib/dictionaries/en/hq.ts:1060 Rp 1,000": "LIVE BUSINESS NUMBER. Third copy of `earnRateRupiah`, this one on the HQ loyalty screen, which is the screen that EDITS the setting — so it can contradict the field directly above it.",
  "apps/web/src/lib/dictionaries/en/hq.ts:585 Rp 100": "LIVE BUSINESS NUMBER. This is REFUND_HQ_THRESHOLD (payment-service config, DEFAULT_REFUND_APPROVAL_THRESHOLD). Change the config and this subtitle keeps quoting Rp 100rb.",
  "apps/web/src/lib/dictionaries/en/hq.ts:852 revenue + 30": "Score weights (70/30), not rupiah — the money regex matched `revenue + 30`. Still a business constant living in copy; low drift risk because the composite score is presentational.",
  "apps/web/src/lib/dictionaries/en/hrFix.ts:770 Rp 2,000": "Presentation. Placeholder text for a free-form note field, illustrating what a note looks like.",
  "apps/web/src/lib/dictionaries/en/opsFix.ts:610 Rp5,000": "LIVE BUSINESS NUMBER. The over-deposit threshold that decides whether a note is mandatory. Copy and rule can drift apart with nobody noticing.",
  "apps/web/src/lib/dictionaries/en/profile.ts:71 Rp 1,000": "LIVE BUSINESS NUMBER. Same `earnRateRupiah` as the help FAQ, hardcoded a second time on the profile onboarding card. Same owed fix.",
  "apps/web/src/lib/dictionaries/id/courierFix.ts:58 Rp 3.000.": "LIVE BUSINESS NUMBER, and possibly a promise nothing keeps: a peak-hour bonus of Rp 3.000 per delivery stated to couriers. Needs confirming against payroll before it is either interpolated or removed.",
  "apps/web/src/lib/dictionaries/id/dashboard.ts:133 Rp2.000": "Presentation. An input-format example (\"mis. -2000 untuk potongan Rp2.000\") teaching the field how to be typed, not quoting a rule.",
  "apps/web/src/lib/dictionaries/id/help.ts:31 Rp 1.000.": "LIVE BUSINESS NUMBER, not presentation. The points earn rate is the per-depot `earnRateRupiah` tunable — /dashboard/loyalist already reads it and interpolates it. This customer FAQ hardcodes Rp 1.000, so a depot that changes the rate leaves this sentence lying. Allowlisted to record it, not to excuse it: fixing it needs the setting fetched on the help screen.",
  "apps/web/src/lib/dictionaries/id/hq.ts:1064 Rp 1.000": "LIVE BUSINESS NUMBER. Third copy of `earnRateRupiah`, this one on the HQ loyalty screen, which is the screen that EDITS the setting — so it can contradict the field directly above it.",
  "apps/web/src/lib/dictionaries/id/hq.ts:589 Rp 100": "LIVE BUSINESS NUMBER. This is REFUND_HQ_THRESHOLD (payment-service config, DEFAULT_REFUND_APPROVAL_THRESHOLD). Change the config and this subtitle keeps quoting Rp 100rb.",
  "apps/web/src/lib/dictionaries/id/hrFix.ts:782 Rp 2.000": "Presentation. Placeholder text for a free-form note field, illustrating what a note looks like.",
  "apps/web/src/lib/dictionaries/id/opsFix.ts:611 Rp5.000": "LIVE BUSINESS NUMBER. The over-deposit threshold that decides whether a note is mandatory. Copy and rule can drift apart with nobody noticing.",
  "apps/web/src/lib/dictionaries/id/profile.ts:69 Rp 1.000": "LIVE BUSINESS NUMBER. Same `earnRateRupiah` as the help FAQ, hardcoded a second time on the profile onboarding card. Same owed fix.",
  "services/depot-service/src/modules/dto/cashier-shift.dto.ts:30 Rp 2.000.": "Presentation. Swagger @ApiProperty example for API documentation — never rendered to a user.",
  "services/depot-service/src/modules/dto/handover.dto.ts:23 Rp 1.240.000": "Presentation. Swagger @ApiProperty example for API documentation — never rendered to a user."
}
