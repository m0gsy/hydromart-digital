// Gallon-issue domain vocabulary (PRD Module 11c). The mirror of retur galon:
// empties handed OUT on deposit. Issues carry no condition (nothing to grade on the
// way out), so — unlike gallon-return — there is no enum here; the record shape lives
// with its repository port. This module is the documented home of the domain concept.
//
// ponytail: manual ledger only. Auto-ingesting issues from an order-completed event
// (order-service coupling) is a deliberate follow-up, not built here.
export {};
