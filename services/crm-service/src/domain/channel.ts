// Broadcast channel (PRD §19 Notification Matrix makes WhatsApp the primary channel).
// Only WhatsApp is supported for now; more channels (push, SMS) can be added later.

export enum CampaignChannel {
  WHATSAPP = 'WHATSAPP',
}
